# Enforce LF line endings in the repository for all text files.
# This ensures consistent diffs and CI behaviour regardless of developer OS.
* text=auto eol=lf

# Explicitly binary — never convert
*.parquet binary
*.h5      binary
*.hdf5    binary
*.png     binary
*.jpg     binary
